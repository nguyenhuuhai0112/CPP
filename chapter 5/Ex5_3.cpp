#include <iostream>
#include <cmath>
using namespace std; 
    int main() {
        double a, n, result;
        cin>>a;
        cin>>n; 
           result = pow(a, n);

           cout << "The result is: " << result << endl;

    return 0;
}
