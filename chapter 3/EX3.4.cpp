#include<iostream>
using namespace std;
      int main(){
      	string chain;
      	cout<<"enter the chain: ";
      	getline(cin,chain);
		int length=chain.length();
		 cout<<"The string length is: "<<length<<endl;
	  return 0;	 
	  }
